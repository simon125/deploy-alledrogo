describe("Test", () => {
  test("Example test", () => {
    const sum = 2 + 2;

    expect(sum).toBe(4);
  });

  test("Example test 1", () => {
    const sum = 2 + 3;

    expect(sum).toBe(5);
  });
});
